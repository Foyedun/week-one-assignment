function sumMix(arr){
var total = 0;
for (let i = 0; i<arr.length; i++)
{
    if (typeof arr[i]==="string")

        total = total +Number([i]);
        
        {
            elseif
            {
            total = total + arr[i];
        }
    }
        return total
    }
}
module.exports = sumMix